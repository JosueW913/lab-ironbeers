<div>
    {{#each this}}
    <img src="{{image_url}}" alt="">
    <h1>{{name}}</h1>
    <h2>{{description}}</h2>
    <h2>{{tagline}}</h2>
    <h2>{{food_pairing}}</h2>
    <h2>{{brewers_tips}}</h2>
    {{/each}}
</div>